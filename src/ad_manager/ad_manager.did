type Chunk_Num = nat64;

type Ad_Info = record {
    "owner": principal;
    "name": text;
    "chunk_num": Chunk_Num;
};

service : {
    "getRandomAdPrincipal": () -> (opt principal) query;
    "createAd" (Ad_Info) -> (Ad_Info);
}

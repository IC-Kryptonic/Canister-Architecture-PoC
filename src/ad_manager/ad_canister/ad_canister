type Chunk_Num = nat64;
type Chunk = blob;

type Ad_Info = record {
    "owner": principal;
    "name": text;
    "chunk_num": Chunk_Num;
};

service : {
    "createAd": (Ad_Info) -> () oneway;
    "insertChunk": (Chunk_Num, Chunk) -> () oneway;
    "getChunk": (Chunk_Num) -> (opt Chunk) query;
}

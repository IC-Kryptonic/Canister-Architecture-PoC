@startuml
Frontend -> Backend: createVideo(videoInfo)
Backend -> DHT: createVideo(videoId, chunkNum)
DHT ->> Bucket: createVideo(videoId, chunkNum)
Backend <-- DHT: createVideo: bucket
Frontend <-- Backend: createVideo: videoInfo

loop chunkNum times
    Frontend ->> Bucket: insertChunk(videoId, chunk)
end
@enduml

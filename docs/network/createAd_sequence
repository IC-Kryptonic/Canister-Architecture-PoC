@startuml
Frontend -> VideoBackend: createVideo(videoInfo)
VideoBackend ->> VideoCanister: createVideo(videoInfo)
Frontend <-- VideoBackend: createVideo: videoInfo

loop chunkNum times
    Frontend ->> VideoCanister: insertChunk(chunk)
end

Frontend ->> AdManager: addAd(adPrincipal)

@enduml

import { makeStyles } from '@material-ui/core';

const uploadStyles = makeStyles({
  uploadContainer: {
    maxWidth: 1280,
    padding: 10,
  },
  dropzone: {
    maxWidth: 400,
  },
  uploadArea: {
    marginTop: 30,
  },
  headerText: {
    fontSize: 40,
  },
});

export { uploadStyles };

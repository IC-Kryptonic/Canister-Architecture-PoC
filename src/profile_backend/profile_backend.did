import "../common_types.did"

service : {
    "get_profile": (principal) -> (opt Profile) query;
    "put_profile": (Profile) -> () oneway;
    "add_like": (principal) -> () oneway;
    "add_comment": (principal) -> () oneway;
}